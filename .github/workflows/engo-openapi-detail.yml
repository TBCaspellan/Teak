name: Engo detailed OpenAPI probe
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/engo-openapi-detail.yml'
      - 'engo_openapi_detail_probe.py'
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      ENGO_API_KEY: ${{ secrets.ENGO_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install requests
      - run: python engo_openapi_detail_probe.py
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: engo-openapi-detail
          path: engo_openapi_detail.json
          if-no-files-found: warn
