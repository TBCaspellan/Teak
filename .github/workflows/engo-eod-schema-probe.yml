name: Engo EOD schema probe
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/engo-eod-schema-probe.yml'
      - 'engo_eod_schema_probe.py'
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      ENGO_API_KEY: ${{ secrets.ENGO_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install requests
      - run: python engo_eod_schema_probe.py
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: engo-eod-schema-probe
          path: engo_eod_schema_probe.json
          if-no-files-found: warn
